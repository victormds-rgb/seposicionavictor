/**
 * Configurações — página estrutural mínima (Sprint 1, Shell).
 * Nenhuma lógica de domínio deste módulo é implementada aqui.
 * Funcionalidade real prevista para a Sprint — (IMPLEMENTATION_PLAN.md).
 */
export default function ConfiguracoesPage() {
  return (
    <div>
      <h1>Configurações</h1>
      <p>Módulo ainda não implementado — previsto para a Sprint —.</p>
    </div>
  );
}
